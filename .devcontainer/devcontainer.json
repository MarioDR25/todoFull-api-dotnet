{
  "name": "C# (.NET) Project",
  "image": "mcr.microsoft.com/devcontainers/dotnet:9.0",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-dotnettools.csharp", 
        "qwtel.sqlite-viewer"
      ]
    }
  },
  "forwardPorts": [5242],
  "postCreateCommand": "dotnet tool install --global dotnet-ef && export PATH=\"$PATH:$HOME/.dotnet/tools\" && dotnet restore",
  "postStartCommand": "export PATH=\"$PATH:$HOME/.dotnet/tools\""
}